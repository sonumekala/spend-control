apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: vendor-app-cluster
  region: us-east-1

nodeGroups:
  - name: app-nodegroup
    instanceType: t3.medium
    desiredCapacity: 3
    volumeSize: 20
    ssh:
      allow: true
